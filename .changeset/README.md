# Changesets

Every pull request that changes the published `tabgrant` CLI should include a
changeset. Run `pnpm changeset`, describe the user-visible change, and choose the
smallest valid SemVer increment. Documentation-only and private-workspace changes
may omit one when the pull-request template explains why.
