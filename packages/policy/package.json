{
  "name": "@tabgrant/policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "test": "vitest run",
    "typecheck": "pnpm --filter @tabgrant/protocol build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@tabgrant/protocol": "workspace:*"
  }
}
